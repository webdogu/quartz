---
title: "03 — 코드 하이라이트"
description: "펜스 블록·여러 언어"
tags:
  - 테스트
  - 코드
draft: false
---

인라인: `const x = 1`.

## TypeScript

```ts
type Page = { slug: string; title: string }

export function build(p: Page): string {
  return `/${p.slug}`
}
```

## Bash

```bash
export PATH="$HOME/.nvm/versions/node/v22.22.2/bin:$PATH"
npx quartz build --serve
```

## JSON

```json
{
  "draft": false,
  "tags": ["테스트"]
}
```

[허브](/테스트/)
